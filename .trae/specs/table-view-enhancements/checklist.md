# 验收清单（Table View Enhancements）

## 颜色
- [x] 表头文字颜色正常（非灰、非 disabled）
- [x] 表格内容文字颜色正常（浅色/深色主题均可读）
- [x] 空态文案可略淡但可读

## 列配置（显示/隐藏）
- [x] 表格模式右上角存在“列配置”按钮
- [x] 可勾选隐藏/显示列
- [x] 至少 2 列可见（低于 2 列时有阻止或自动修正）
- [x] 按页面保存：home/contacts/companies/tasks 互不影响
- [x] 重启 App 后仍生效
- [x] 远端 UI config 更新后，本地 override 仍作为覆盖（未知列自动忽略）

## 冻结列与滚动
- [x] 横向滚动时首列固定
- [x] 纵向滚动时左右两侧行始终对齐
- [x] 前两列宽度较紧凑，文本过长以省略号展示

## 真机回归（Android）
- [x] 首页切换表格/列表正常
- [x] Contacts/Companies/Tasks 切换表格/列表正常
- [x] 切换列显示不会导致红屏/崩溃

## 筛选与排序
- [x] 所有可见字段可设置筛选并生效
- [x] 所有可见字段可升序/降序排序
- [x] 清空筛选/排序后恢复默认结果
- [x] 筛选弹窗为 BottomSheet，并支持 contains/equals/startsWith
- [x] 多列筛选叠加（AND）结果正确，且单列 Clear 不影响其它列
- [x] 连续快速点击筛选不红屏，且 BottomSheet 每次都可见（不出现只弹输入法）

## 列顺序调整
- [x] 列配置面板支持拖拽调整前后位置
- [x] 调整后表格列顺序即时生效
- [x] 重启后列顺序保持（按 pageKey）

## 分页与汇总
- [x] 表格底部展示总记录数/当前区间/页码信息
- [x] 支持每页条数设置（10/20/50/100）
- [x] 支持输入页码跳转并处理越界

## Twenty 对齐（冻结选择列）
- [x] 横向滚动时冻结列为“窄选择列”（checkbox + icon）
- [x] 冻结选择列宽度接近 Twenty（明显小于普通字段列）

## 构建与静态检查
- [x] 已运行 flutter analyze
- [x] 已运行 flutter build apk --debug
